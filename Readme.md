# Secure File Vault

## How to install

```bash
python -m venv .venv
source .venv/bin/activate
pip install --require-hashes --only-binary=all -r requirements.txt
```

## How to run

```bash
python src/main.py
```
